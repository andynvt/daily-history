name: daily_history
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_html: ^1.3.0
  http: ^0.13.4
  provider: ^6.0.1
  shared_preferences: ^2.0.9
  timeline_tile: ^2.0.0
  url_launcher: ^6.0.17

dev_dependencies:
  flutter_lints: ^1.0.0
flutter:
  uses-material-design: true

